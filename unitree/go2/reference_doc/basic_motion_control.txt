"本文介绍如何使用 unitree_sdk2 调用底层接口开发 Go2 机器人，相关的代码文件为 unitree_sdk2/example/go2 路径下的 go2_low_level.cpp。在本例程中会示范如何调用底层接口控制四足机器人的右前腿摆动。运行该例程的方法可参考《快速开始》篇，本文在此不再赘述。\n\n程序解析\nCustom类\n\n在 Custom 类中了定义与 DDS 通信相关的类与函数，还有一些与控制腿摆动的变量。\n\n成员变量\n与腿部控制相关的变量\n名称\t类型\t说明\nqInit\tfloat类型数组\t本例子中用于记录右前腿各关节的初始关节角度\nqDes\tfloat类型数组\t本例子中用于表达右前腿各关节的期望关节角度\nsin_mid_q\tfloat类型数组\t本例子中用于指定右前腿各关节周期往复运动中的中间关节角度\nKp\tfloat类型数组\t本例子中用于指定右前腿各关节的关节空间控制kp值\nKd\tfloat类型数组\t本例子中用于指定右前腿各关节的关节空间控制Kd值\n与通信相关的变量\n名称\t类型\t说明\n\nlowcmd_publisher\n\tChannelPublisherPtr<unitree_go::msg::dds_::LowCmd_>\t底层命令发送对象,调用该对象的Write函数会向Go2机器人发送一次底层控制命令\nlowstate_subscriber\tChannelSubscriberPtr<unitree_go::msg::dds_::LowState_>\t底层状态信息订阅对象，底层状态包含电机信息、IMU数据、宇树手柄信息等\nlowCmdWriteThreadPtr\tThreadPtr\tDDS相关的底层命令发送线程指针\ncmd\tunitree_go::msg::dds_::LowCmd_\tDDS相关的底层命令结构体，是最终通过DDS发送给Go2机器人，区别于LowCmd low_cmd\nstate\tunitree_go::msg::dds_::LowState_\tDDS相关的底层状态结构体\nlow_cmd\tUNITREE_LEGGED_SDK::LowCmd\tUNITREE_LEGGED_SDK类型的关于底层控制命令的结构体\nlow_state\tUNITREE_LEGGED_SDK::low_state\tUNITREE_LEGGED_SDK类型的关于底层状态信息的结构体\n\n与通信相关的变量主要可以分为两种类型：UNITREE_LEGGED_SDK 类型 和  unitree_go::msg::dds_ 类型，它们的关系见于下图:\n\n在本例程中，关于腿部摆动的控制用户逻辑首先用 UNITREE_LEGGED_SDK 类型变量进行表达，然后通过一些例程中提供的函数将其转换为 DDS 相关的类型，然后才将命令发至 Go2 机器人。这是因为 A1、Go1 等机器人的 SDK也采用了 UNITREE_LEGGED_SDK 类型，此举为方便之前的用户延续与移植。\n\n成员函数\n\nInitLowCmd()\n\nvoid Custom::InitLowCmd()\n    {\n        low_cmd.head[0] = 0xFE; \n        low_cmd.head[1] = 0xEF;\n        low_cmd.levelFlag = 0xFF;\n        low_cmd.gpio = 0;\n    \n        for(int i=0; i<20; i++){\n            low_cmd.motorCmd[i].mode = (0x01);   // motor switch to servo (PMSM) mode\n            low_cmd.motorCmd[i].q = (PosStopF);\n            low_cmd.motorCmd[i].Kp = (0);\n            low_cmd.motorCmd[i].dq = (VelStopF);\n            low_cmd.motorCmd[i].Kd = (0);\n            low_cmd.motorCmd[i].tau = (0);\n        }\n    }\n\nInitLowCmd() 该函数用于初始化设置 LowCmd 类型的 low_cmd 结构体。该函数放置于 Custom 类的构造函数运行一次即可。下面为其中一些要点：\n\nLowCmd 类型中的 head 成员 表示帧头，此帧头将用于 CRC 校验。head 、levelFlag、gpio 等按例程所示设置为默认值即可。\n\nLowCmd 类型中有 20 个 motorCmd 成员，每一个成员的命令用于控制 Go2 机器人上相对应的一个电机，但 Go2 机器人上只有 12 个电机，故仅有前 12 个有效，剩余的8个起保留作用。\n\nlow_cmd.motorCmd[i].mode = (0x01); 此行命令中将 motorCmd 成员的 mode 变量设置为 0x01，0x01 表示将电机设置为伺服模式。如果用户在调试过程中发现无法控制 Go2 机器人的关节电机，请检查变量的值是否为0x01。\n\nLowStateMessageHandler\n\n此函数的作用为关于底层状态信息接收的 DDS 回调函数，DDS 机制会以一定频率触发该回调函数。每次触发该回调函数都将更新一次底层状态信息。\n\nLowCmdwriteHandler()\n\n此函数的作用为发送底层控制命令的回调函数，DDS 会以一定频率触发该回调函数。此回调函数的前大部分代码表示计算右前小腿摆动控制命令的用户逻辑，后小部分代码将控制命令发送至 Go2 机器人。\n\nvoid LowCmdwriteHandler()\n{\n     motiontime++;\n    \n        for(int i=0; i<12; i++){\n          printf(\"%f \", state.motor_state()[i].q());\n        }\n        printf(\"\\n\");\n    \n        // if( motiontime >= 100){\n        if (motiontime >= 0){\n            // first, get record initial position\n            if (motiontime >= 0 && motiontime < 20){\n                qInit[0] = state.motor_state()[FR_0].q();\n                qInit[1] = state.motor_state()[FR_1].q();\n                qInit[2] = state.motor_state()[FR_2].q();\n            }\n            // second, move to the origin point of a sine movement with Kp Kd\n            if (motiontime >= 10 && motiontime < 400) {\n                rate_count++;\n                double rate = rate_count / 200.0; // needs count to 200\n                Kp[0] = 5.0; Kp[1] = 5.0; Kp[2] = 5.0;\n                Kd[0] = 1.0; Kd[1] = 1.0; Kd[2] = 1.0;\n    \n                qDes[0] = jointLinearInterpolation(qInit[0], sin_mid_q[0], rate);\n                qDes[1] = jointLinearInterpolation(qInit[1], sin_mid_q[1], rate);\n                qDes[2] = jointLinearInterpolation(qInit[2], sin_mid_q[2], rate);\n            }\n            double sin_joint1, sin_joint2;\n            // last, do sine wave\n            float freq_Hz = 1;\n            // float freq_Hz = 5;\n            float freq_rad = freq_Hz * 2 * M_PI;\n            float t = dt * sin_count;\n            if (motiontime >= 400){\n                sin_count++;\n                sin_joint1 = 0.6 * sin(t * freq_rad);\n                sin_joint2 = -0.9 * sin(t * freq_rad);\n                qDes[0] = sin_mid_q[0];\n                qDes[1] = sin_mid_q[1] + sin_joint1;\n                qDes[2] = sin_mid_q[2] + sin_joint2;\n            }\n    \n            low_cmd.motorCmd[FR_2].q = qDes[2];\n            low_cmd.motorCmd[FR_2].dq = 0;\n            low_cmd.motorCmd[FR_2].Kp = Kp[2];\n            low_cmd.motorCmd[FR_2].Kd = Kd[2];\n            low_cmd.motorCmd[FR_2].tau = 0;\n        }\n        low_cmd.crc = crc32_core((uint32_t *)&low_cmd, (sizeof(LowCmd)>>2)-1);\n    \n        lowCmd2Dds(low_cmd, cmd);\n        lowcmd_publisher->Write(cmd);\n }\n\n上述代码块中的三个if语句可将用户逻辑分为三部分：\n\nif (motiontime >= 0 && motiontime < 20) -> 记录初始关节角度\nif (motiontime >= 10 && motiontime < 400) -> 运动至往复运动起始角度\nif (motiontime >= 400) -> 开始往复运动\n\n下面将进行讲解：\n1.记录初始关节角度\n\nif (motiontime >= 0 && motiontime < 20)\n{\n  qInit[0] = state.motor_state()[FR_0].q();\n  qInit[1] = state.motor_state()[FR_1].q();\n  qInit[2] = state.motor_state()[FR_2].q();\n}\n\n该段代码表示当 motiontime 小于 20 时，会读取右前腿(FR)的机身关节(FR_0)、大腿关节(FR_1)、 小腿关节(FR_2)的关节角度值，并分别赋值给 qInit 数组中的元素。可以总结此段代码的作用是记录右前腿的初始位置, 如下图所示\n\n\n运动至往复运动起始角度\nif (motiontime >= 10 && motiontime < 400) {\n    rate_count++;\n    double rate = rate_count / 200.0; // needs count to 200\n    Kp[0] = 5.0; Kp[1] = 5.0; Kp[2] = 5.0;\n    Kd[0] = 1.0; Kd[1] = 1.0; Kd[2] = 1.0;\n    \n    qDes[0] = jointLinearInterpolation(qInit[0], sin_mid_q[0], rate);\n    qDes[1] = jointLinearInterpolation(qInit[1], sin_mid_q[1], rate);\n    qDes[2] = jointLinearInterpolation(qInit[2], sin_mid_q[2], rate);\n  }\n}\n\n此段代码的作用是计算控制命令 qDes， 该控制命令会驱动右前腿各关节缓慢地从 qInit 数组指定的角度 运动到 sim_mid_q 数组中指定的关节角度，如下图所示\n\n\n开始往复运动\ndouble sin_joint1, sin_joint2;\n// last, do sine wave\nfloat freq_Hz = 1;\n// float freq_Hz = 5;\nfloat freq_rad = freq_Hz * 2 * M_PI;\nfloat t = dt * sin_count;\nif (motiontime >= 400)\n{\n  sin_count++;\n  sin_joint1 = 0.6 * sin(t * freq_rad);\n  sin_joint2 = -0.9 * sin(t * freq_rad);\n  qDes[0] = sin_mid_q[0];\n  qDes[1] = sin_mid_q[1] + sin_joint1;\n  qDes[2] = sin_mid_q[2] + sin_joint2;\n}\n    \nlow_cmd.motorCmd[FR_2].q = qDes[2];\nlow_cmd.motorCmd[FR_2].dq = 0;\nlow_cmd.motorCmd[FR_2].Kp = Kp[2];\nlow_cmd.motorCmd[FR_2].Kd = Kd[2];\nlow_cmd.motorCmd[FR_2].tau = 0;\n\n此段代码的作用是计算控制命令 qDes， 该控制命令会驱动右前腿中的小腿关节在 sim_mid_q 数组中指定的关节角度附近摆动。如下图所示，右前小腿将会在sin_mid_q[2] ± 0.9 rad 范围内摆动，如下图所示\n\n\nlow_cmd.crc = crc32_core((uint32_t *)&low_cmd, (sizeof(LowCmd)>>2)-1);\nlowCmd2Dds(low_cmd, cmd);\nlowcmd_publisher->Write(cmd);\n\n此段代码中第一行首先计算了 CRC 校验码。第二行代码通过 lowCmd2Dds() 函数将 UNITREE_LEGGED_SDK::low_cmd 类型的 low_cmd变量 转换至 与 DDS 相关的unitree_go::msg::dds_::LowCmd_ 类型的 cmd 变量。最后一行代码表示调用 lowcmd_publisher 的 Write() 函数将 控制命令 发送给 Go2 机器人。\n\nDDS 转换函数\n\n以下是一系列与 DDS 相关的转换函数。当用户表达控制逻辑时，主要对UNITREE_LEGGED_SDK::LowCmd  和 UNITREE_LEGGED_SDK::LowState 这两种类型的变量进行操作，因为这两种类型可读性更高。而实际给Go2机器人发命令或获取状态信息时，则 通过 unitree_go::msg::dds_::LowCmd_ 和 unitree_go::msg::dds_::LowState_ 这两种类型的结构体。\n故需要通过一些转换函数将这两种类型的结构体进行转换。用户重点关注 lowCmd2Dds() 和 dds2LowState() 转换函数即可。而对于中间函数，则可不必过分关注实现细节，使用时直接复制粘贴即可。\n\n名称\t说明\nvoid dds2Imu(unitree_go::msg::dds_::IMUState_& dds, IMU& raw)\t关于 IMU 状态信息的转换函数，将unitree_go::msg::dds_::IMUState_转换至  UNITREE_LEGGED_SDK::IMU，为中间函数。\nvoid dds2BmsState(unitree_go::msg::dds_::BmsState_& dds, BmsState& raw)\t关于电池状态信息的转换函数，将unitree_go::msg::dds_::BmsState_转换至  UNITREE_LEGGED_SDK::BmsState，为中间函数。\nvoid dds2MotorState(std::array<unitree_go::msg::dds_::MotorState_, 20>& dds, MotorState* raw)\t关于电机状态信息的转换函数，将unitree_go::msg::dds_::MotorState_转换至  UNITREE_LEGGED_SDK::MotorState，为中间函数。\nvoid dds2LowState(unitree_go::msg::dds_::LowState_& dds, LowState& raw)\t将UNITREE_LEGGED_SDK::LowState 转换至unitree_go::msg::dds_::LowState_。\nvoid motorCmd2Dds(MotorCmd* raw, std::array<unitree_go::msg::dds_::MotorCmd_, 20>& dds)\t关于电机控制命令的转换函数，将UNITREE_LEGGED_SDK::MotorCmd转换至unitree_go::msg::dds_::MotorState_，为中间函数。\nvoid bmsCmd2Dds(BmsCmd& raw, unitree_go::msg::dds_::BmsCmd_& dds)\t关于电池管理命令的转换函数，将UNITREE_LEGGED_SDK::BmsCmd转换至unitree_go::msg::dds_::BmsCmd_ ，为中间函数。\nvoid lowCmd2Dds(LowCmd& raw, unitree_go::msg::dds_::LowCmd_& dds)\t将UNITREE_LEGGED_SDK::LowCmd 转换至unitree_go::msg::dds_::LowCmd_。\nvoid crc32_core(uint32_t* ptr, uint32_t len)\tcrc校验函数，为中间函数。"