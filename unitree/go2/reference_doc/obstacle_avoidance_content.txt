"ObstaclesAvoidClient 类\n\nObstaclesAvoidClient 是 Go2 避障服务提供的 Client，通过 ObstaclesAvoidClient 可以通过 RPC 方式实现对 Go2 避障功能的开关和开关状态获取。\n\n接口错误码列表：\n\n暂无\n\n类与接口描述：\n类名\t构造与析构\nObstaclesAvoidClient\texplicit ObstaclesAvoidClient(); ~ObstaclesAvoidClient();\n函数名\tSwitchSet\n函数原型\tint32_t SwitchSet(bool enable)\n功能概述\t设置避障功能开启或关闭。\n参数\tenable：设置 true 为开启，false 为关闭。\n返回值\t调用成功返回 0，否则返回相关错误码。\n备注\t\n函数名\tSwitchGet\n函数原型\tint32_t SwitchGet(bool& enable);\n功能概述\t获取避障功能是否开启。\n参数\tenable：返回值， true 为开启，false 为关闭。\n返回值\t调用成功返回 0，否则返回相关错误码。\n备注\t\n函数名\tUseRemoteCommandFromApi\n函数原型\tint32_t UseRemoteCommandFromApi(bool isRemoteCommandsFromApi)\n功能概述\t抢夺遥控器的速度指令控制权。\n参数\tisRemoteCommandsFromApi：设置 true 为开启抢夺，false 为关闭抢夺。\n返回值\t调用成功返回 0，否则返回相关错误码。\n备注\t需将isRemoteCommandsFromApi设为true，才能通过api控制机器狗避障\n函数名\tMove\n函数原型\tint32_t Move(float x, float y, float yaw);\n功能概述\t避障模式下的移动控制。\n参数\tx：机身x方向速度指令，限制为-1.5~+1.5(m/s)； y：机身y方向速度指令，限制为-1~+1(m/s)； yaw：机身yaw角速度指令，限制为-1.57~+1.57(rad/s)。\n返回值\t调用成功返回 0，否则返回相关错误码。\n备注\t需将isRemoteCommandsFromApi设为true，才能通过Move函数控制机器狗避障\n函数名\tMoveToIncrementPosition\n函数原型\tint32_t MoveToIncrementPosition(float x, float y, float yaw);\n功能概述\t避障模式下的移动控制。\n参数\tx：机身x方向的位置增量，限制为-2.0~+2.0(m)； y：机身y方向的位置增量，限制为-2.0~+2.0(m)； yaw：机身yaw角增量，限制为-1.57~+1.57(rad)。\n返回值\t调用成功返回 0，否则返回相关错误码。\n备注\t需将isRemoteCommandsFromApi设为true，才能通过MoveToIncrementPosition函数控制机器狗避障\n函数名\tMoveToAbsolutePosition\n函数原型\tint32_t MoveToAbsolutePosition(float x, float y, float yaw);\n功能概述\t避障模式下的移动控制。\n参数\tx：世界坐标系x方向的绝对位置； y：世界坐标系y方向的绝对位置； yaw：世界坐标系下的yaw。\n返回值\t调用成功返回 0，否则返回相关错误码。\n备注\t需将isRemoteCommandsFromApi设为true，才能通过MoveToAbsolutePosition函数控制机器狗避障\n\n例程：\n\n#include <unitree/robot/go2/obstacles_avoid/obstacles_avoid_client.hpp>\n\nusing namespace unitree::common;\nusing namespace unitree::robot;\nusing namespace unitree::robot::go2;\n\nint main()\n{\n    unitree::robot::ChannelFactory::Instance()->Init(0, \"enx00e04c36141b\");//enx00e04c36141b为网口号，用户根据自身情况修改\n    unitree::robot::go2::ObstaclesAvoidClient sc;\n    sc.SetTimeout(5.0f);\n    sc.Init();\n    sc.SwitchSet(true);//开启避障\n    usleep(1000000);\n    sc.UseRemoteCommandFromApi(true);//抢夺遥控器的速度控制权\n    sc.Move(1.0,0.0,0.0);//沿着机体x方向以1m/s的速度运动，碰到障碍物自动避障\n    int tem = 0;\n    while (true)\n    {\n      usleep(1000000);\n      if(tem<=5)\n      {\n        tem++;\n      }\n      else\n      {\n        sc.UseRemoteCommandFromApi(false);//5秒后关闭\n        sc.SwitchSet(false);//5秒后关闭\n      }\n    }\n    return 0;\n}"